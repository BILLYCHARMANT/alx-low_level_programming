#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_binary(unsigned long int n)
{
	unsigned long int a;

	n = scanf("%lu input any number", a);
	printf("%lu binary of n:", n);
}
